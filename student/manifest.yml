---
env:
    SPRING_PROFILES_DEFAULT: cloud
applications:
- name: student
  path: build/libs/student-0.0.1-SNAPSHOT.jar
  services: [ eureka-service ]
  host: student-${random-word}
  domain: pezapp.io
